{
  "_doc": "Pipeline capacity limits (substrate A/B). Edit to taste; used by core/capacity.py.",
  "max_parallel_projects": 3,
  "max_created_projects": 20,
  "max_supervisors": 2,
  "max_concurrent_agents_per_project": 3,
  "provider_limits": {
    "openrouter": {
      "tokens_per_min": 400000,
      "requests_per_min": 60
    },
    "opencode-go": {
      "tokens_per_min": 400000,
      "requests_per_min": 120
    }
  },
  "global_budget": {
    "soft_tokens": 100000000,
    "hard_tokens": 300000000
  },
  "notes": "max_parallel_projects = concurrent running projects; max_created_projects = total registered (queued+defined); max_supervisors = how many portfolio instances may run at once (scale-out)."
}